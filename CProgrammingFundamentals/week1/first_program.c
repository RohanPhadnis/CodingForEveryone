// include is the directive for importing libraries
// stdio.h is the standard input/output library in c
#include <stdio.h>

// main method (must return an int)
int main() {
    printf("Hello, World!\n");  // printf is the output command
    return 0;
}

// gcc filename.c --> compiles the code to an a.out file
// gcc -o filename.exe filename.c --> compiles the code to a filename.exe file
// ./filename.exe --> executes the code
